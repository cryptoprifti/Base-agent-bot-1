{
  "buildCommand": "pnpm --filter @workspace/basebot run build && cp -r artifacts/basebot/dist/public dist",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
