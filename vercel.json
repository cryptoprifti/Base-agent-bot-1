{
  "buildCommand": "pnpm --filter @workspace/basebot run build && mkdir -p public && cp -r artifacts/basebot/dist/public/* public/",
  "outputDirectory": "public",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
